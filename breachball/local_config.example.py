"""Per-machine settings. Copy this file to local_config.py (gitignored) and
edit it there — local_config.py is never committed, so cabinet vs. desk
settings don't get flip-flopped in the game's actual history."""

# Flip on once running on the actual cabinet with spinners attached; leave
# off for keyboard playtesting on a regular machine.
USE_MOUSE_SPINNERS = False
